apiVersion: apps/v1
kind: Deployment
metadata:
  name: dotnet-api
spec:
  replicas: 2
  selector:
    matchLabels:
      app: dotnet-api
  template:
    metadata:
      labels:
        app: dotnet-api
    spec:
      containers:
        - name: dotnet-api
          image: acrakstf98872.azurecr.io/webapiacrtfjenkinsdocker:latest
          ports:
            - containerPort: 7192
---
apiVersion: v1
kind: Service
metadata:
  name: dotnet-api-service
spec:
  type: LoadBalancer
  selector:
    app: dotnet-api
  ports:
    - protocol: TCP
      port: 80
      targetPort: 7192
